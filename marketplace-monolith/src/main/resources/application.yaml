spring:
  application:
    name: marketplace-monolith
  data:
    mongodb:
      uri: mongodb://mongo:27017/marketplace
      database: marketplace

server:
  servlet:
    context-path: /backend
  port: 8080
